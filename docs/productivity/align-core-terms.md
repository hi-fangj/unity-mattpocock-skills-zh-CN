Quickstart:

```bash
npx skills add hi-fangj/unity-mattpocock-skills-zh-CN --skill=align-core-terms
```

## What it does

`align-core-terms` 是 grilling 之前的术语对齐阶段。它从口述或混乱需求中提取会影响理解的核心术语，逐项确认定义、范围、同义词和一词多义问题，直到用户确认共享词汇。

它不负责澄清完整需求，也不替用户决定冲突的业务含义。术语统一后，再运行 `/grill-me` 或 `/grill-with-docs` 进入需求访谈。
